{
  "name": "RTGS-Bank-Portal",
  "private": true,
  "devDependencies": {
    "cross-env": "^7.0.3",
    "lerna": "3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start --parallel --stream"
  }
}
