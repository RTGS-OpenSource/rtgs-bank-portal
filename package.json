{
  "name": "RTGS-Bank-Portal",
  "private": true,
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm-run-all --parallel start-client start-server",
    "start-client": "yarn workspace rtgs-bank-portal-client start",
    "start-server": "yarn workspace rtgs-bank-portal-server start"
  }
}
